<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>List of Genres</h1>
    <% @genres.each do |genre| %>
      <li><%= link_to genre.name, genre_songs_path(genre) %></li>
    <% end %>
  </body>
</html>
