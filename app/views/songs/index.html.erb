<h1></h1>

<ul>
<% @songs.each do |song| %>
    <li><%= song.title %></li>
<% end %>
</ul>

<% if user_signed_in? %>
    <%= link_to 'Log Out', destroy_user_session_path, method: :delete %>
<% end %>
